
spring:
  jpa.hibernate.ddl-auto: validate
  liquibase.change-log: classpath:liquibase-changeLog.xml
  application.name: spring-security-5
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
